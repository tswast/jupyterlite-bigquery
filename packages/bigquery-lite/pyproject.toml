[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bigquery-lite"
version = "0.1.0"
description = "A placeholder for bigquery-lite"
dependencies = []
